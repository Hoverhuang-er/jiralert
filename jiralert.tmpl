{{ define "jira.summary" }}{{ .Status | toUpper }} AlertName:{{ .GroupLabels.SortedPairs.Values | join "-" }}{{ end }}

{{ define "jira.description" }}{{ range .Alerts.Firing }}Labels:
{{ range .Labels.SortedPairs }} - {{ .Name }} = {{ .Value }}
{{ end }}
Annotations:
{{ range .Annotations.SortedPairs }} - {{ .Name }} = {{ .Value }}
{{ end }}
MoreDetail: {{ .externalURL }}
{{ end }}{{ end }}
